server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log warn;

    sendfile on;
    tcp_nopush on;
    aio threads;

    location / {
        autoindex on;
        try_files $uri =404;
    }
}

